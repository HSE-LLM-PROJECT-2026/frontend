apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.nginxConfigName" . }}
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  default.conf: |
{{ .Files.Get "files/nginx.conf" | indent 4 }}
